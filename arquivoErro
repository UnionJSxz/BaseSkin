@echo off
set servicename=NomeDoServico
set waittime=60

:loop
echo Reiniciando o serviço %servicename%...

REM Parar o serviço
sc stop %servicename%
timeout /t 5 /nobreak >nul

REM Iniciar o serviço
sc start %servicename%
timeout /t %waittime% /nobreak >nul

goto loop